# acp2026
Solutions to Level 1 of ACP. Contains the C programs, bats-core tests, and GDB output for the programs.

To use the tests, compile each file such that its name is the same as the source C file, but has a `.out` extension
